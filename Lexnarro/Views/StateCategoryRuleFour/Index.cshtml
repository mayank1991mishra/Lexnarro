
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>State Category With Rule Four Mapping</h2>

<p>
    @Html.ActionLink("Create New", "Create", new { }, htmlAttributes: new { @class = "btn btn-primary" })
</p>
<section class="panel">
    <div class="panel-body">
        @*@using (Html.BeginForm())
        {
            <p>
                <div class="row">
                    <div class="col-md-2">Filter by state : </div>
                    <div class="col-md-4">
                        @Html.DropDownList("stateid", new SelectList(ViewBag.States), 
                       "-Select state-", htmlAttributes: new { @class = "form-control col-md-4" })
                     </div>
                    <div class="col-md-4">
                        <input type="submit" value="Search" class="btn btn-info" />
                    </div>
                </div>
            </p>
        }*@
        <table class="table table-hover" id="dataTable">
            <thead>
                <tr>
                    <th></th>
                    <th>
                        State
                    </th>
                    <th>
                        Category
                    </th>
                    <th>
                        Rule Four
                    </th>
                    <th>
                        Start Date
                    </th>
                    <th>
                        End Date
                    </th>
                    <th>
                        State
                    </th>
                    <th>Action</th>
                </tr>
            </thead>
        </table>        
    </div>
</section>

@section Javascript{
    @Html.Raw(ViewBag.message)
}

@section scripts
    {
    <script>
        $(function () {
            var t = $('#dataTable').DataTable({
                theme: 'bootstrap',
                responsive: true,
                autoWidth: false,

                "columnDefs": [{
                    "searchable": false,
                    "orderable": false,
                    "targets": 0
                }],
                "order": [[1, 'asc']],

                "ajax": {
                    "url": '/StateCategoryRuleFour/GetData',
                    "type": 'get',
                    "datatype": "json"
                },
                "columns": [
                    { "data": null },
                    { "data": "State" },
                    { "data": "Category" },
                    { "data": "Rule" },
                    { "data": "StartDate" },
                    { "data": "EndDate" },
                    { "data": "Status" },
                    {
                        "data": "Id", render: (function (data, type, row) {

                            return '<a href="/StateCategoryRuleFour/Edit/' + data + '" class="btn btn-success">Edit</a>' +
                                '|<a href="/StateCategoryRuleFour/Details/' + data + '" class="btn btn-info">Details</a>' +
                                '|<a href="/StateCategoryRuleFour/Delete/' + data + '" class="btn btn-danger">Delete</a>';
                        })
                    }
                ]
            });
            t.on('order.dt search.dt', function () {
                t.column(0, { search: 'applied', order: 'applied' }).nodes().each(function (cell, i) {
                    cell.innerHTML = i + 1;
                });
            }).draw();
        });
    </script>
}
